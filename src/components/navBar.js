import React from 'react';
import { NavLink } from 'react-bootstrap';




const NavBar = () =>(
    <header>
        <nav>
            <NavLink to='/'>
            </NavLink>
        </nav>
    </header>
);

export default NavBar;